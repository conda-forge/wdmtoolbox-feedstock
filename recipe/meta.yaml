{% set name = "wdmtoolbox" %}
{% set version = "16.0.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/wdmtoolbox-{{ version }}.tar.gz
  sha256: e29642c2cabbc3b3d6dab387221123cbafea9d6cbe3655a2f1a3bae8de3aec2b

build:
  entry_points:
    - wdmtoolbox = wdmtoolbox.wdmtoolbox:main
  script: {{ PYTHON }} -m pip install . -vv  # [not win]
  number: 0

requirements:
  build:
    - {{ compiler('fortran') }}  # [not win]
    - {{ compiler('c') }}
    - clang                      # [win]
    - flang                      # [win]
    - lld                        # [win]
    - meson-python
  host:
    - meson
    - meson-python
    - numpy
    - pip
    - python
    - wheel
  run:
    - {{ pin_compatible('numpy') }}
    - cltoolbox >=2.0.0,<3.0.0
    - filelock
    - pandas
    - pint !=0.21
    - pint <0.22         # [py<3.9]
    - pint-pandas <=0.3  # [py<3.9]
    - pint-pandas        # [py>=3.9]
    - python
    - python-build
    - python-dateutil
    - tabulate
    - toolbox_utils >=5.0.0,<6.0.0    

test:
  imports:
    - wdmtoolbox
  commands:
    - pip check
    - wdmtoolbox --help
  requires:
    - pip

about:
  summary: Read and write Watershed Data Management (WDM) files
  home: https://pypi.org/project/wdmtoolbox/
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - timcera
